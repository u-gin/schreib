import 'package:flutter/material.dart';

/// The app's colours, in one place.
///
/// Replaces a `colorFromHex` helper that was copy-pasted into three screens
/// and handed the same literal each time.
abstract final class AppColors {
  /// The page background everything sits on.
  static const Color background = Color(0xFFF2F8FC);
}
